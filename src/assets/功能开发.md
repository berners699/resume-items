### 用户登录模块

- form 表单，表单验证，v-model,事件绑定,prop，flex 布局，store, persist持久化, 路由守卫,
  路由白名单, 路由重定向

### 首页模块

- 首页布局，flex布局, element-ui container布局, slot插槽，父子组件通信
  嵌套路由，原子化css, aside侧边栏，menu菜单组件，drawer组件，less, ref，reactive,template语法，
  模块化，es6, ts, 组件封装，组件复用, 组件通信,主题切换,通用组件,

### 用户列表模块

-菜单，路由改造，动态路由，动态菜单，动态权限控制， table组件渲染表格，mock数据，分页，搜索

### 角色模块

- 管理员，

### 权限模块

- 性能优化： 压缩 vite-compression-plugin, rolllup-plugin-brotli, manualChunks,chunkFileNames, entryFileNames, assetFileName, css预处理器，css模块化, 打包优化，代码分割，懒加载，按需加载，tree shaking, 缓存策略，ssr, pwa, 骨架屏, 性能分析工具，webpack-bundle-analyzer, 代码质量检查，eslint, prettier, 业务代码，三方代码外联，vite-plugin-html.

### 公共部分

- 底部导航栏，顶部导航栏，flex布局，父子组件通信

### 移动端开发

- 设计稿基本是750px，
- 适配方案：rem布局
- vant
- postcss-px-to-viewport px转vw,
- postcss-pxtorem px转rem
- 底部安全区适配
- 顶部安全区适配
- 移动端的难点和亮点：
    1. 适配问题：rem布局，vw布局
    2. 移动端适配问题：1px边框，点击穿透
    3. 形成移动端最佳模板， 在团队内部推广

## CI/CD 持续集成（以git action为例子）

- 项目打包后的 dist 文件夹放在 nginx, tomacat, apache 服务器上
- ci/cd就是将这个步骤自动化,常见的方式有：
    - jekins
    - gitlab ci/cd
    - github action

## 技术栈

### 前端技术栈

1. Vue2 + vuex + vue-router
2. axios
3. element-ui
4. flex布局
5. webpack
6. es6+
7. vue-cli脚手架
8. vue-devtools
9. vue-resource
10. vue-router
11. vuex
12. vue-i18n
13. vue-lazyload
14. vue-infinite-scroll
15. vue-touch
16. vue-awesome-swiper
17. vue-echarts
18. vue-quill-editor
19. vue-amap
